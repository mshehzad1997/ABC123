@inject Microsoft.AspNetCore.Identity.SignInManager<AppointmentDoctor.Models.ApplicationUser> signInManager
@inject AppointmentDoctor.Models.ApplicationDbContext context
@{
    if (signInManager.IsSignedIn(User))
    {
        <form method="post" id="logoutForm" asp-action="Logoff" asp-controller="Account">
            <ul class="nav navbar-nav">
                <li class="nav-link">Hello,@User.Identity.Name!</li>
                <li><a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
            </ul>
        </form>
    }
    else
    {
        @*<li>@Html.ActionLink("Sign up", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerlink" })</li>
        <li>@Html.ActionLink("Sign In", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginlink" })</li>*@
<ul class="nav navbar-nav">
    <a class="nav-link text-black-50" id="registerLink" asp-controller="Account" asp-action="Register">Register</a>
    <a class="nav-link text-black-50" id="loginLink" asp-controller="Account" asp-action="Login">Sign In</a>
</ul>
    }
}